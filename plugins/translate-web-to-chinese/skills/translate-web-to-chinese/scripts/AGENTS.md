# AGENTS Guide: skills/translate-web-to-chinese/scripts

Generated by `codebase-frontmatter-summary`.

## Scope

Contains 4 file(s) and 0 subdirectory(s). Highlights: `crawl_site.py`: Python module defining `build_arg_parser`, and `main`. `render_relations.py`: Python module defining `build_arg_parser`, and `main`. `site_common.py`: Python module defining `normalize_url`, `scope_prefix`, `in_scope`, and `short_id`.

## Files
- `crawl_site.py`: Python module defining `build_arg_parser`, and `main`.
- `render_relations.py`: Python module defining `build_arg_parser`, and `main`.
- `site_common.py`: Python module defining `normalize_url`, `scope_prefix`, `in_scope`, and `short_id`.
- `translate_site.py`: Python module defining `build_arg_parser`, `main`, `translated_outputs_exist`, and `run_single_page_translation`.

## Directories
- None.
