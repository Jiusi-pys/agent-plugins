# AGENTS Guide: skills/translate-url-to-chinese/scripts

Generated by `codebase-frontmatter-summary`.

## Scope

Contains 4 file(s) and 0 subdirectory(s). Highlights: `codex_sdk_bridge.mjs`: Mjs file: import process from "node:process";. `package-lock.json`: JSON object with top-level keys `name`, `lockfileVersion`, `requires`, and `packages`. `package.json`: JSON object with top-level keys `name`, `private`, `type`, and `dependencies`.

## Files
- `codex_sdk_bridge.mjs`: Mjs file: import process from "node:process";. Frontmatter skipped: no safe frontmatter style.
- `package-lock.json`: JSON object with top-level keys `name`, `lockfileVersion`, `requires`, and `packages`. Frontmatter skipped: no safe frontmatter style.
- `package.json`: JSON object with top-level keys `name`, `private`, `type`, and `dependencies`. Frontmatter skipped: no safe frontmatter style.
- `translate_url.py`: Python module defining `AssetReference`, `PageParser`, `build_arg_parser`, and `main`.

## Directories
- None.
