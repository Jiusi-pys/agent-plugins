# AGENTS Guide: skills/translate-url-to-chinese

Generated by `codebase-frontmatter-summary`.

## Scope

Contains 1 file(s) and 2 subdirectory(s). Highlights: `SKILL.md`: Markdown document "Translate URL To Chinese". Use this skill when the job is a single page, not a whole site. `agents/`: Contains 1 file(s) and 0 subdirectory(s). Highlights: `openai.yaml`: YAML file with top-level keys `interface`. `scripts/`: Contains 4 file(s) and 0 subdirectory(s). Highlights: `codex_sdk_bridge.mjs`: Mjs file: import process from "node:process";. `package-lock.json`: JSON object with top-level keys `name`, `lockfileVersion`, `requires`, and `packages`. `package.json`: JSON object with top-level keys `name`, `private`, `type`, and `dependencies`.

## Files
- `SKILL.md`: Markdown document "Translate URL To Chinese". Use this skill when the job is a single page, not a whole site.

## Directories
- `agents/`: Contains 1 file(s) and 0 subdirectory(s). Highlights: `openai.yaml`: YAML file with top-level keys `interface`.
- `scripts/`: Contains 4 file(s) and 0 subdirectory(s). Highlights: `codex_sdk_bridge.mjs`: Mjs file: import process from "node:process";. `package-lock.json`: JSON object with top-level keys `name`, `lockfileVersion`, `requires`, and `packages`. `package.json`: JSON object with top-level keys `name`, `private`, `type`, and `dependencies`.
