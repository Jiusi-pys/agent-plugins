# AGENTS Guide: .codex-plugin

Generated by `codebase-frontmatter-summary`.

## Scope

Contains 1 file(s) and 0 subdirectory(s). Highlights: `plugin.json`: JSON object with top-level keys `name`, `version`, `description`, and `author`.

## Files
- `plugin.json`: JSON object with top-level keys `name`, `version`, `description`, and `author`. Frontmatter skipped: no safe frontmatter style.

## Directories
- None.
