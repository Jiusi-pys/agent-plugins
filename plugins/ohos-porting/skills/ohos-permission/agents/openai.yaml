interface:
  display_name: "OHOS Permission"
  short_description: "Configure DSoftBus session permissions"
  default_prompt: "Use $ohos-permission to update or verify this DSoftBus session permission setup and keep the JSON, deployment, reboot, and functional validation steps correct."
