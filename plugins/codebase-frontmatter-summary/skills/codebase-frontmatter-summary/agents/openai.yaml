interface:
  display_name: "Codebase Frontmatter Summary"
  short_description: "Scan a tree, add frontmatter to files, and write a summary for each directory"
  default_prompt: "Use $codebase-frontmatter-summary to scan this codebase bottom-up, refresh managed file frontmatter, and generate directory summaries."
