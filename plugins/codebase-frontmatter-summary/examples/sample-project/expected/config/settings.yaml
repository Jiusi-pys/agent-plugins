# codex-file-meta: begin
# relative_path: "config/settings.yaml"
# language: "yaml"
# summary: "YAML file with top-level keys `app_name`, `environment`, and `features`."
# symbols: ["app_name", "environment", "features"]
# generated_by: "codebase-frontmatter-summary"
# codex-file-meta: end

app_name: example-project
environment: demo
features:
  summaries: true
  frontmatter: true
